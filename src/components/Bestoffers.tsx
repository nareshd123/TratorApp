import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Bestoffers = () => {
    return (
        <View style={{ paddingHorizontal: 30, borderWidth: 1, alignItems: 'center' }}>
            <Text style={{ fontSize: 28, color: '#000', textTransform: 'uppercase', fontFamily: 'Poppins-Thin' }}>Our best offers</Text>
        </View>
    )
}

export default Bestoffers

const styles = StyleSheet.create({})